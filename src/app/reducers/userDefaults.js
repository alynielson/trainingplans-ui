
const initialState = {
  firstDayOfWeek: 1,
  mainActivityTypes: ["Run", "Bike"]
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}